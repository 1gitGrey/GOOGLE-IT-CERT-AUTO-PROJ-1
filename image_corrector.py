#!/usr/bin/python3 
import os
from PIL import Image
import sys

# define global variables
WORKING_DIR = os.paths.abspath(os.path.curdir)
TARGET_DIR = ''
def getImagePath(parent_directory):
    target_directory = WORKING_DIR + parent_directory
    return target_directory

# open
for img in getImagePath("images")
# rotate
PIL.Image.Image.rotate(90)
# resize?
PIL.Image.Image.resize((,))
#save


def
